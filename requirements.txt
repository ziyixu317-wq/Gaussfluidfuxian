torch>=1.12.1
plyfile
numpy
scipy
matplotlib
tqdm
imageio
opencv-python
tensorboard
# Optional (auto-detected): pytorch3d or faiss-gpu for faster KNN
# If neither is available, falls back to pure PyTorch torch.cdist
